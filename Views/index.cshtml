<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="container">
      <h1>Address Book</h1>
      @if (@Model.Count == 0)
      {
        <h2>Name/Address Index</h2>
        <h3>Input a person and their info to continue:</h3>
        <h3><a href="/contact/new">Add a new contact</a></h3>
      }
      else
      {
        @foreach (var friend in Model)
        {
        <h3><a href='/contact/@contact.GetId()'>@contact.FriendPerson()</a></h3>
        }
        <h3><a href="/contacts/new">Add a new contact</a></h3>
        <form action="/contacts/delete_contacts" method="post">
          <button class="button btn" type="submit">Delete your Index</button>
        </form>
      }
    </div>
  </body>
</html>
</head>
